package com.feetfit.server.repository;

import com.feetfit.server.domain.ReportMetricScore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReportMetricScoreRepository extends JpaRepository<ReportMetricScore, Long> {
}
