spring:
  datasource:
    url: jdbc:mysql://feetfit-db.cloquck0gnqu.us-west-2.rds.amazonaws.com:3306/feetfit?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: feetfit
    password: feetfit1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: "q9dgU72LFwp03/beozN8fB3vUbOdrMwGJmuXrAt7BbM="

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

swagger:
  local-server-url: ${SWAGGER_LOCAL_SERVER_URL:http://localhost:8080}
  deploy-server-url: ${SWAGGER_DEPLOY_SERVER_URL:http://54.184.58.176}

file:
  upload:
    bucket-name: ${S3_BUCKET_NAME:project5-42-oregon-feetfit-s3}
    region: ${AWS_REGION:us-west-2}
    public-url-prefix: ${S3_PUBLIC_URL_PREFIX:https://project5-42-oregon-feetfit-s3.s3.us-west-2.amazonaws.com}
